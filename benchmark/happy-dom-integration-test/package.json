{
	"name": "@happy-dom/integration-test",
	"private": true,
	"version": "0.0.0",
	"license": "MIT",
	"author": "David Ortner",
	"description": "Private package used for integration testing of Happy DOM.",
	"type": "module",
	"publishConfig": {
		"access": "restricted"
	},
	"scripts": {
		"test": "bun test test --path-ignore-patterns '**/browser-exception-observer/**' && bun test/browser-exception-observer/BrowserExceptionObserver.test.js",
		"test:debug": "cd ./test && ls | node --inspect-brk"
	},
	"devDependencies": {
		"happy-dom": "20.11.11",
		"@types/node": ">=20.0.0",
		"express": "^4.20.0"
	},
	"engines": {
		"node": ">=20.0.0"
	}
}
