// Facade seam placeholder: tree mutation extension.
// Reserved by T20A for T24C, which implements it after T20A archives:
// `appendChild`/`insertBefore`/`removeChild`/`replaceChild` wrapper
// conversion and return objects per js/facade/CONTRACT.md. Do not write from
// any other task.

export const seam = Object.freeze({
  id: "facade/extensions/mutation",
  owner: "T24C",
  gate: "T24",
  status: "placeholder",
});
