# Raw CPU-profile binaries stay local, matching the 01–03 evidence precedent
# (the main tree tracks zero *.cpuprofile). Every excluded file is bound by
# path/bytes/sha256 to its committed batch record in profiles/INDEX.json.
profiles/**/*.cpuprofile
