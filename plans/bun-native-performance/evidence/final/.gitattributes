# Raw captured command output is evidence: bytes must never be rewritten
# (bun/cargo summaries contain padded trailing spaces inside table rows).
# The root .gitattributes applies the same -whitespace rule to the vendored
# happy-dom tree; this scopes it to the task-05 command ledger.
commands/*.log -whitespace
